<%- include ('partials/header.ejs') %>
<%- include ('partials/homenavbar.ejs') %> 
<body>
    <div class="container mt-3">
        <div class="p-5 mb-4 bg-light rounded-3">
            <div class="container-fluid py-5">
              <h1 class="display-5 fw-bold"> Bem Vindo(a)</h1>
              <p class="col-md-8 fs-4">Este é um simples blog feito com Nodejs e EJs durante estudos da linguagem para aprimorar conhecimentos.</p>
              <button class="btn btn-primary btn-lg" type="button">Leia mais</button>
            </div>
          </div>
        <% articles.forEach(article => { %>
            
            <div class="card mb-3">
                <div class="card-header">
                    <h2><%= article.title%></h2>
                </div>
                <div class="card-body">
                    <a href="/<%= article.slug %>" class="btn btn-success">Ler artigo</a>
                </div>
            </div>

       <% }) %>
       <div class="row">
        <div class="col col-12">
            <a style="float: right;" >Next</a>
        </div>
       </div>
    </div>
</body>
<%- include ('partials/footer.ejs') %>